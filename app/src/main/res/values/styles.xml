<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">serif</item>
        <item name="fontFamily">serif</item>

        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>

        <item name="android:actionMenuTextColor">@color/textColorBlack</item>
        <item name="android:actionMenuTextAppearance">@style/UserProfileToolbarMenu</item>

    </style>

    <style name="UserProfileToolbarMenu" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textStyle">bold</item>
    </style>

</resources>
